<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_auth"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layoutDirection="rtl"
    android:fitsSystemWindows="true"
    tools:openDrawer="start">

    <include layout="@layout/activity_auth_content" />

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/activity_auth_navigationView"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@color/White"
        android:fitsSystemWindows="true"
        android:theme="@style/NavigationViewTheme"
        app:headerLayout="@layout/activity_auth_header"
        app:itemTextColor="@color/Grey"
        app:itemTextAppearance="@style/NavigationTextAppearance"
        app:itemIconSize="@dimen/_16sdp"
        app:itemIconPadding="@dimen/_16sdp"
        app:itemHorizontalPadding="@dimen/_16sdp"
        app:itemIconTint="@drawable/sel_nav_icon"
        app:itemBackground="@drawable/sel_nav_draw"
        app:menu="@menu/menu_drawer">

        <include layout="@layout/activity_auth_footer" />

    </com.google.android.material.navigation.NavigationView>

</androidx.drawerlayout.widget.DrawerLayout>